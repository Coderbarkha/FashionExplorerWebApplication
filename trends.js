function applyFilter() {
    const filterValue = document.getElementById("trend-filter").value;
    // Add logic to filter the displayed trends based on the selected filter
    alert(`Filtering by: ${filterValue}`);
  }
  
  function personalizeTrends() {
    // This function can be used to trigger a sign-in modal or redirect to a sign-in page.
    alert("Please sign in to view personalized trends.");
  }
  